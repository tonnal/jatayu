# secrets
.env
*.key

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# node / next
node_modules/
web/.next/
web/out/
.DS_Store

# engine outputs — generated artifacts (mock in demo, real after live run).
# The graded deliverables get copied into deliverables/ explicitly for submission.
data/cache/
data/output/

# but DO track the graded deliverables
!deliverables/
